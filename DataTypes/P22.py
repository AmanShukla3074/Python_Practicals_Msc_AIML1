# 22.	replace all occurrences of "Python" with "Java" in a string.
# Answer:
# Use str.replace for a straightforward replacement.

s = input("Enter a string (may contain 'Python'): ")
print("After replace:", s.replace("Python", "Java"))
