# 3.	Write a Python program to convert an integer to binary, octal, and hexadecimal.
# Answer:
# I use bin(), oct(), and hex() to show conversions.

n = int(input("Enter an integer: "))
print("Binary:", bin(n))
print("Octal:", oct(n))
print("Hexadecimal:", hex(n))
